# Copyright (c) Iputils Project, 2018-2020
#
# Update NAME.pot:
# meson . builddir && ninja NAME-pot -C builddir
#
# Update *.po files
# meson . builddir && ninja NAME-update-po -C builddir

import('i18n').gettext(name, args: '--directory=' + meson.current_source_dir())

